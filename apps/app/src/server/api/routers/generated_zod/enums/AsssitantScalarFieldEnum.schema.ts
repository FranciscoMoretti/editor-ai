/* eslint-disable */
import { z } from 'zod';

export const AsssitantScalarFieldEnumSchema = z.enum(["assistant_id", "graph_id", "config_id", "created_at", "updated_at", "metadata", "version", "name", "user_id"])
